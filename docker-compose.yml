version: '3.8'
services:
#  headacheapp:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    ports:
#      - "8080:8080"
#    command: ["java", "-jar", "/app.jar"]
#    environment:
#        - SPRING_PROFILES_ACTIVE=development
#        - JAVA_OPTS=-Xmx512m -Xms256m
#        - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/headache_db
#        - SPRING_DATASOURCE_USERNAME=root
#        - SPRING_DATASOURCE_PASSWORD=root-pw
#    depends_on:
#      - db

  db:
    image: mysql:9.3.0-oraclelinux9
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root-pw
      MYSQL_DATABASE: headache_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
    mysql_data: